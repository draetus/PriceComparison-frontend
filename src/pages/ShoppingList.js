import React, { Component } from 'react';
import { Typography } from '../components';
import { ShoppingListContainer } from '../features';

class ShoppingList extends Component {

    render() {
        return (
            <ShoppingListContainer />
        )
    }
}

export default ShoppingList;